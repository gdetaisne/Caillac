services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: hg
      POSTGRES_PASSWORD: hg
      POSTGRES_DB: heritage_geraud
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:

